export function DocumentationTab() {
  return (
    <p className="text-sm text-muted-foreground">
      Ainda não há documentos vinculados diretamente a serviços — o schema atual não relaciona a
      tabela de documentos a serviços. A Phase 9 (APIs/Docs) trata essa lacuna quando a feature de
      Documentation for implementada de fato.
    </p>
  );
}
